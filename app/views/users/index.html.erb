 <h1>Users</h1>
 <table>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.name %>
    <%= link_to "👀", user_path(user.id)%></td>
    <!---フォローボタンのパーシャル _follow_form.html.erb --->
    <td><%= render 'follow_form', user: user %></td>
  </tr>
  <% end %>
  </table> 