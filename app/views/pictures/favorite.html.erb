<h1>Favorite</h1>
    <br>
    <p>
      <span class="glyphicon glyphicon-heart" aria-hidden="true"></span> <%= pluralize(@picture.favorites.count, "like", locale: :en)%>
    </p>
  <table class="detail">
    <tr>
      <td><strong>User:</strong></td>
    
      <td>
        <% if @picture.favorite_users.present? %>
            <% @picture.favorite_users.each do |favorite| %>
                <%= favorite.name %>
            <% end %>
        <% end %>
      </td>
      <td colspan="4"></td>
    </tr>
    
    <tr>
      <td><strong>Liked at:</strong></td>
      <td><%= @picture.favorite_users.first.created_at.strftime('%Y/%m/%d %H:%M') if @picture.favorite_users.present? %></td>
    </tr>
  </table>

  

  